# 研究成果の2方式登録：実装報告

## 変更範囲

対象は `my_website` のみ。既存研究成果2件とPDFは削除せず保持した。
既存の `static/equipment_reservation.css` の作業中の変更には触れていない。
依存パッケージの追加、コミット、pushは行っていない。

変更したファイル:

- `main.py`：初期化、登録、一覧、PDF配信、削除処理。
- `templates/addform.html`：名前・紹介文・方式選択・テンプレート選択の構成。
- `static/addform.js`：DB定義による入力欄生成、表示切替、登録結果確認、二重送信防止。
- `static/addform.css`：追加フォームのスタイル。
- `templates/studylist.html`：生徒一覧の形式別表示。
- `templates/teacher/studylist.html`：教員一覧の形式別表示。
- `templates/admin/studies.html`：管理者一覧の形式別表示。
- `database/database.db`：既存DBへのカラム・テーブル追加、初期テンプレート投入。

新規ファイル:

- `studies.py`：研究成果用DB接続、スキーマ初期化、テンプレート取得、PDF形式・パス検証。
- `tests/test_studies.py`：登録・権限・削除・移行・異常系テスト16件。
- `tests/test_addform.cjs`：フォーム生成・送信処理のJavaScriptテスト5件。
- `RESEARCH_REGISTRATION.md`：本報告。

## DB構造

| テーブル | カラム | 役割 |
| --- | --- | --- |
| `study_template` | `id` PK、`seed_key` UNIQUE、`name`、`active` | テンプレート本体と利用可否。追加テンプレートの `seed_key` はNULLでもよい |
| `study_template_field` | `id` PK、`template_id` FK、`label`、`position`、`required` | 項目名・並び順・必須判定。現時点ではすべて文字入力 |
| `study` | 下記参照 | 登録された研究成果の共通情報 |
| `study_field_value` | `study_id` FK、`field_id` FK、`value`。前2つの複合PK | 各研究成果の項目ごとの入力値 |

`study` の全カラム:

| カラム | 内容 |
| --- | --- |
| `id` | 自動採番の主キー |
| `name` | 研究成果名。新規登録では必須。テンプレートの「テーマ」とは別保存 |
| `introduce` | 紹介文。新規登録では必須 |
| `filename` | PDF表示名。テンプレート方式では空文字 |
| `pdfpath` | uploads内のPDF保存名。テンプレート方式では空文字 |
| `userid` | セッションから取得した投稿生徒ID |
| `time` | 投稿日時（新規登録は日本時間） |
| `registration_type` | `pdf` / `template`。CHECK制約。旧データは `pdf` |
| `template_id` | 使用テンプレートの外部キー。PDF方式はNULL |
| `submission_key` | 同じフォーム送信の重複防止用キー。UNIQUEインデックス。旧データはNULL |

初期化は `PRAGMA table_info(study)` で確認して不足カラムだけ追加する。
研究成果の書き込み・削除用接続では `PRAGMA foreign_keys = ON` を使用する。
研究成果を削除すると入力値は `ON DELETE CASCADE` で削除される。
テンプレートから項目への関係もCASCADEだが、提出済み研究成果が参照するテンプレート・項目はRESTRICTで物理削除を防ぐ。
投稿生徒の既存テーブルには主キー制約がないため、`userid` に外部キーは追加せず、登録時に生徒の存在を確認する。

## 保存処理

共通処理:

1. 生徒ログイン・セッション上の生徒ID・CSRF・送信トークンを確認。
2. 登録方式をサーバーで判定。SQLはプレースホルダーを使用。
3. DBトランザクション内で送信キーを確認。同一送信の再試行には既存の登録結果を返す。
4. コミット完了後にJSONで成功を返す。画面はHTTP成功とJSONの `ok` を確認して初めて成功表示する。

PDF方式:

- 拡張子、PDFバージョンヘッダー、末尾の `startxref` / `%%EOF` を検証。申告MIMEだけには依存しない。
- 利用者のファイル名は表示用にのみ使用し、実ファイル名にはUUIDを使用。
- 一時ファイルへ保存してから確定ファイルへ移動し、その後DBをコミット。
- 保存・DB処理の失敗時はロールバックし、一時ファイルと保存済みファイルを削除する。
- 閲覧URLは従来と同じ `/uploads/{研究成果ID}.pdf`。DBの保存パスに解決して配信する。
- テンプレート方式・存在しない研究成果のPDFアクセスは404。

テンプレート方式:

- 有効なテンプレートと項目定義をDBから再取得する。
- 選択したテンプレートに存在しない項目IDは拒否する。
- 必須判定にはDBの `required` を使う。クライアントから項目名は受け取らない。
- 空欄を含む全項目の値を、研究成果本体と同一トランザクションで保存する。
- 名前・紹介文はPDF方式・テンプレート方式ともに必須。未送信・空文字・空白文字のみの入力はサーバー側でも拒否する。初期9項目はすべて空欄可。アプリ独自の文字数上限は設けていない。既存データは変更しない。

削除:

- 教員の学校単位の存在確認・権限条件・CSRF検証を維持。
- 管理者のログイン確認を維持。
- PDF方式はDB削除後に関連PDFを削除。対象はuploads直下のPDFに限定。
- テンプレート方式は研究成果削除に連動して入力値も削除。

## 初期テンプレートと将来の拡張

初期テンプレート名は「研究レポート（基本）」。指定の9項目を、すべて任意入力として投入する。
`seed_key = basic-research-v1` のUNIQUE制約と `INSERT OR IGNORE` で重複を防ぎ、初回作成時だけ項目を投入する。
起動時に既存テンプレート名や項目を上書きしない。

将来の管理画面は `study_template` / `study_template_field` を編集するAPIと画面として追加できる。
`get_templates()` と登録処理は項目数に依存せず、追加・並び替え・必須設定をDBから反映する。
登録済みの本文は項目IDを参照するため、使用中の項目を変更する場合は過去の提出物への影響を考慮する必要がある。
使用中テンプレートを削除したい場合は `active = 0` で非公開化し、大きな構成変更は別テンプレートとして追加する運用が可能。
過去の項目名を固定保存するバージョン管理は今回追加していない。

## 実施した確認と結果

- `.venv\Scripts\python.exe -m unittest discover -s tests -v`：32件成功（研究成果16件、既存の通知・コミュニティ16件）。
- `node --test tests/test_addform.cjs`：5件成功。
- Python構文チェック、`node --check static/addform.js`、`git diff --check`：成功。
- 両方式の名前・紹介文の必須検証、テンプレート本文の全項目空欄登録、入力値・研究名の独立保存、日本語のmultipart送信を確認。
- 別テンプレート・異なる項目数・必須設定・不正テンプレート/項目・CSRF・未認証を確認。
- PDF登録・配信、不正PDF拒否、保存失敗とDB失敗時の後片付けを確認。
- 同一送信の再試行と同時送信で重複しないことを確認。
- 追加画面と生徒・教員・管理者一覧をJinjaでレンダリングし、PDFリンクとテンプレート方式の表示を確認。
- 教員・管理者の削除、入力値CASCADE削除、教員による他校データの削除拒否を確認。
- JavaScriptのDOMテストで方式切替、9項目/20項目生成、成功前の表示抑止、二重送信防止、通信/サーバー失敗時の再試行を確認。
- 実DBの既存テーブル13個について、Git上の作業開始時のDBにある全既存カラムの値と比較し、一致を確認。研究成果2件も維持。
- 実DBの `PRAGMA integrity_check` は `ok`、研究成果関連の外部キー違反なし。テンプレート1件・項目9件を確認。

ブラウザによる目視・実操作テストは実施していない。自動テストは一時DBと一時アップロードフォルダを使用する。
既存テストと同様に `main` のimport時には実DBの通常の初期化が実行されるが、テスト投稿・削除は実DBに対して行わない。

## 未実装・注意点

- 指定どおり、管理者のテンプレート管理画面、本文閲覧専用ページ、途中保存・下書き・編集、画像・グラフ・表の挿入は未実装。
- 一覧ではテンプレート方式に「テンプレート形式（本文表示機能は未実装）」を表示する。
- PDF検証はヘッダーと末尾構造の基本検証であり、全オブジェクトの解析・描画検証やマルウェア検査は行わない。
- DBとファイルシステムを完全に同一トランザクションにはできない。プロセスの強制終了やファイル削除権限エラーではファイルが残る可能性がある。削除失敗はログに記録する。
- 現行のPDF公開範囲と、研究成果管理以外の認証・予約・通知処理は変更していない。
- 同じ送信キーは既存の結果を返す。別の研究成果を登録する場合は成功後の「続けて別の研究成果を追加」から新しいフォームを開く。

## ユーザー確認項目

- [ ] `/addform` で登録方式を切り替え、表示・余白・操作感を確認。
- [ ] 実際に使うPDFを登録して一覧から開けることを確認。
- [ ] テンプレート9項目を入力し、成功メッセージと各ロールの一覧表示を確認。
